class Candidate {
  bool isQualifiedToApply(int age, double workExperience, String others) => true;

  // bondavi is very poor (sorry...).
  bool shouldApply(bool lovesMoney) => !lovesMoney;

  List howToApply = ["Direct message on Twitter", "Direct message on Instagram", "Mail to 'info@bondavi.jp'"];
}